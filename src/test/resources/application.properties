# 단위 테스트는 유레카 서버 없이 돈다 — 등록/조회 시도 차단.
# (main application.yml과 병행 로드되고 키 단위로만 우선하므로 라우트 정의는 그대로 살아있다.)
# lb:// 라우트는 인스턴스 0개면 503이 되는데, 테스트는 401 여부만 보거나
# BOARD_SERVICE_URI로 직접 스텁을 주입하므로 영향 없다.
eureka.client.enabled=false
